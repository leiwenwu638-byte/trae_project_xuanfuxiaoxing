# 统一换行符策略
#
# 目标：
#   - 源码 / 配置文件统一使用 LF（避免 Windows 下大量 LF/CRLF warning
#     以及 git diff 出现无意义换行符改动）；
#   - Windows 脚本（.bat / .cmd / .ps1）保留 CRLF（Windows 解释器依赖
#     CRLF 行尾，否则可能出现"无法识别"或"找不到命令"）；
#   - 显式声明 `text=auto` 让 Git 自行判定文本文件，避免误把二进制当文本。
#
# 参考：
#   https://docs.github.com/en/get-started/getting-started-with-git/configuring-git-to-handle-line-endings

# 默认：所有文本文件入库时统一 LF
* text=auto eol=lf

# Windows 脚本：保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
